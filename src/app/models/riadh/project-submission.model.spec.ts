import { ProjectSubmission } from './project-submission.model';

describe('ProjectSubmission', () => {
  it('should create an instance', () => {
    expect(new ProjectSubmission()).toBeTruthy();
  });
});
